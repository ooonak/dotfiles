02062021 Casper

# Install dependencies
sudo apt install exuberant-ctags cscope

# Cloning our dotfiles to another machine
git clone https://github.com/ooonak/dotfiles.git

cd ~/dotfiles
chmod +x makesymlinks.sh
./makesymlinks.sh

# Initial setup of Vim plugins

1. Get Vundle
git clone https://github.com/gmarik/Vundle.vim.git ~/.vim/bundle/Vundle.vim

2. As vimrc is setup, let Vundle handle installation of the rest of the plugins.
Open vim and execute :PluginInstall
